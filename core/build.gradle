apply plugin: "java"

sourceCompatibility = 1.6
dependencies {
    implementation project(path: ':gdx-sqlite')
    implementation project(path: ':gdx-sqlite-android')
}
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets.main.java.srcDirs = [ "src/" ]


eclipse.project {
    name = appName + "-core"
}
